// String, number, unefined, null, boolean
const nome = 'Mario'; //String
const nome1 = 'Mario'; //String
const nome2 = `Mario`; //String

const num1 = 10; //number
const num2 = 10.52; //number

let nomeAluno; // indefined -> sem ocupar posição de memoria
const sobrenomeAluno = null; // Nulo -> sem ocupar posição de memoria

const aprovado = false; // boolean = true or false (logico)

//console.log(typeof nome, typeof sobrenomeAluno, typeof nomeAluno, typeof aprovado)
const a =[1, 2];
const b = a;

console.log